FROM minio/minio:latest

# Tạo thư mục lưu trữ dữ liệu
RUN mkdir -p /mnt/data

# Expose cổng cho MinIO API và Console
EXPOSE 9000 9001

# Cấu hình ENTRYPOINT và CMD rõ ràng cho Render
ENTRYPOINT ["minio"]
CMD ["server", "/mnt/data", "--console-address", ":9001"]